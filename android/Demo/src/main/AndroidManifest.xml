<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.signdemo1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_v1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_v1"
        android:supportsRtl="true"
        android:name=".DemoApplication"
        android:theme="@style/Theme.SignDemo1">
        <activity
            android:name=".ChooseActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--Intent filter to capture authorization code response from the default browser on the device calling back to our app after interactive sign in -->
        <activity android:name="com.microsoft.identity.client.BrowserTabActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="@string/ms_sign_in_host"
                    android:path="@string/ms_sign_in_path"
                    android:scheme="@string/ms_sign_in_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".test.TestActivity" />
        <activity android:name=".IdTokenActivity" />
        <activity android:name=".googletest.GoogleTestActivity" />
        <activity android:name=".googletest.GoogleTest1Activity" />

        <meta-data
            android:name="com.microsoft.quick.auth.signin.clientId"
            android:value="@string/ms_sign_in_client_id" />
        <meta-data
            android:name="com.microsoft.quick.auth.signin.redirectUri"
            android:value="@string/ms_sign_in_redirect_uri" />
    </application>

</manifest>